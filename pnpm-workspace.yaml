# Per E2E_MVP_PLAN.md §10 Q13 (LOCKED):
#   pnpm workspaces cover the TypeScript packages only.
#   mobile/ is a standalone Flutter peer directory, driven by the plain `flutter` CLI,
#   and is DELIBERATELY absent from this list. Do not add it.
packages:
  - 'web'
  - 'api'
  - 'shared'
  - 'infra'
