/**
 * @parimaan/shared — cross-platform TypeScript types, generated GraphQL types,
 * and Zod validation schemas. See SYSTEM_DESIGN.md §9.3.
 *
 * Populated in W1. This placeholder exists so `tsc --noEmit` has an input file.
 */

export const SHARED_PACKAGE_NAME = '@parimaan/shared' as const;
